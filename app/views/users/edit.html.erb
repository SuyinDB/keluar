<h2>Update your profile<h2>

	<%= simple_form_for @user do |form| %>

	<%= form.input :username %>
	<%= form.input :email %>

	<%= form.input :firstname %>
	<%= form.input :surname %>

	<%= form.input :address %>

	<%= form.button :submit, "Update" %>

<% end %>